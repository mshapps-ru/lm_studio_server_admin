# ToDo лист по управлению Windows‑службой
1. **Добавить обработку аргументов** `-createService`, `-deleteService`, `-startService`, `-stopService` в `Program.Main`. 
2. **Реализовать вспомогательный класс ServiceHelper** (или статические методы в Program.cs):
   - Получение пути к исполняемому файлу (`Assembly.GetExecutingAssembly().Location`).
   - Методы: `CreateService`, `DeleteService`, `StartService`, `StopService` с использованием `sc`.
3. **Проверить корректность пути** в Release‑сборке и при наличии пробелов/символов. 
4. **Тесты**:
   - Создание службы (`-createService`).
   - Запуск и остановка (`-startService`, `-stopService`).
   - Удаление (`-deleteService`).
5. **Обновить README**: добавить раздел *Windows Service Management* с примерами команд.
6. **Логирование**: убедиться, что логи пишутся в `logs/app.log` при работе как службы.
7. **Права администратора**: уточнить и документировать необходимость прав для выполнения `sc`.
8. **Обработка ошибок**: выводить понятные сообщения при отказе (например, служба уже существует).
9. **Опционально**: добавить параметры `DisplayName`, `Description` в команду `sc create`.
10. **Документация**: обновить план и комментарии к коду.
