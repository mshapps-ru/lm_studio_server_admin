Разработай план доработок:
1) Проанализируй документацию по текущему проекту в папке docs, при необходимости посмотри код проекта и не приступай к следующим пунктам пока все не изучишь
2) Задавай при необходимости вопросы и отображай ответы в плане
3) Давайте cделаем загрузку списка моделей полученных из lm studio server из эндпоинта /v1/models. Список полученных моделей давай сохранять в файле config.json и в аналогичном формате как получаем из /v1/models. В файле давай хранить в формате:
	"LmStudioModelList": [
		{
			"id": "gpt-oss-20b",
			"object": "model",
			"owned_by": "organization_owner"
		}
	]
4) Список моделей давай обновлять раз в 5 минут автоматически по расписанию в бэкенде. Если список моделей изменился, только тогда список моделей обновляем в файле config.json. При ошибках получения списка моделей из эндпоинта /v1/models ничего не делаем, просто пишем в лог что не удалось получить список моделей.

5) Также давай в config.json добавим хранения дефолтных параметров для запуска моделей командой lms load "имя модели" "параметры для запуска"

	"LmStudioModelDefaultLoadParameter": [
		{
			"contextWindow": 100000,
			"ttl": 600
		}
	]

Ниже пример запуска модели gpt-oss-20b с параметрами "contextWindow": 100000 и ttl 10 минут

lms load gpt-oss-20b --context-length 100000 --ttl 600

6) Необходимо в файле config.json индивидуальные параметры запуска моделей моделей и использовать их для запуска если указаны:

	"LmStudioModelLoadParameterList": [
		{
			"id": "gpt-oss-20b",
			"contextWindow": 120000,
			"ttl": 1200
		}
	]

Учти что некоторые индивидуальные параметры могут пересекаться с параметрами по умолчанию. При старте нужно использовать в приоритете параметры указанные в параметре "LmStudioModelLoadParameterList"

7) Сделай ToDо лист чтобы потом идти по плану при реализации
8) Сохрани подробный план с конкретными шагами без обрезания в файл tz/06-load-models-list-and-parameters.md только после того как у меня все ответы на вопросы получишь.
9) Не приступай к доработкам пока я план не посмотрю
10) Проверь что каждый пункт текущего промта есть в плане доработок
11) Этот промт сохрани без обрезания и перевода на английский язык в файл tz/06-load-models-list-and-parameters-prompt.md
12) Согласованный ToDо лист сохрани в файл tz/06-load-models-list-and-parameters-todo.md
13) После завершения подготовки плана всех работ, выведи в чат содержимое файлов tz/06-load-models-list-and-parameters.md и tz/06-load-models-list-and-parameters-todo.md